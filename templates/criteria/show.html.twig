{% extends 'base.html.twig' %}

{% block title %}Criteria{% endblock %}

{% block body %}

    {% include 'navbar.html.twig' %}

    <h1>Criteria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ criterion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ criterion.name }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ criterion.data }}</td>
            </tr>
            <tr>
                <th>IndexReference</th>
                <td>{{ criterion.indexReference }}</td>
            </tr>
            <tr>
                <th>Scale</th>
                <td>{{ criterion.scale }}</td>
            </tr>
            <tr>
                <th>Coefficient</th>
                <td>{{ criterion.coefficient }}</td>
            </tr>
            <tr>
                <th>Methodology</th>
                <td>{{ criterion.methodology }}</td>
            </tr>
            <tr>
                <th>Pin</th>
                <td>{{ criterion.pin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_criteria_index') }}">back to list</a>

    <a href="{{ path('app_criteria_edit', {'id': criterion.id}) }}">edit</a>

    {{ include('criteria/_delete_form.html.twig') }}
{% endblock %}
