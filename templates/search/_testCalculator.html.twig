<div>
    <h1 class="text-gray-900 text-base no-underline hover:no-underline font-extrabold text-xl"> Test an address</h1>
    <p class="mt-4"> Enter an address to discover its green score: </p>
    <form method="get" action="{{ path('app_search') }}" onsubmit="return confirm('Etes-vous sur de vouloir tester cette adresse?');">
    <label for="adress"></label><br>
    <input class="w-full lg:w-2/5 shadow appearance-none border rounded py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" type="text" id="adress" name="adress" placeholder="27 Bd de Stalingrad, 44000 Nantes">
    <div>
        <button type="submit" class="mt-6 bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">Search</button>
    </div>
    </form>

    {% if notes %}
        <div>
            <p class="mt-8 mb-8"> Green score for the address: {{adress}} </p>
            <div class="w-1/6 bg-transparent text-center text-green-700 font-semibold py-2 px-4 border border-green-500">
                <p>
                    {{ notes.globalNote }} / 100
                </p>
            </div>
        </div>
    {% endif %}
</div>
